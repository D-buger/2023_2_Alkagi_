using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Replay : MonoBehaviour
{
    private void Start()
    {
        //P_ReplaySave replaySave = default;
        //replaySave.message = "Replay Message Test";
        //Client.TCP.SendPacket2(E_PACKET.REPLAY_SAVE_REQUEST, replaySave);

        //P_ReplayLoadRequest replayLoad = default;
        //replayLoad.playID = 115;
        //Client.TCP.SendPacket2(E_PACKET.REPLAY_LOAD_REQUEST, replayLoad);
    }
}
